<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MVVMPracticeMAUI.Views.ListDetailDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:MVVMPracticeMAUI.ViewModels"
    x:DataType="vm:ListDetailDetailViewModel">
    <ScrollView>
        <VerticalStackLayout Margin="12">
            <Label FontSize="Header" Text="{Binding Item.Title}" />
            <Label FontSize="Default" Text="{Binding Item.Description}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
